node_modules/
coverage/
release/
*.log
.env
.DS_Store
.codex-marketplace-install.json

# dist/ is intentionally NOT ignored.
# Harnesses install plugins without running lifecycle scripts, so there is no
# build step on the adopter's machine (plan 15.5). The bundle must be
# committed for a Git-sourced install to work at all.
COMMITMSG.txt
