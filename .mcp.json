{
  "mcpServers": {
    "factory": {
      "command": "/bin/sh",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/bin/factory-mcp",
        "${CLAUDE_PLUGIN_ROOT}/dist/mcp-server.js"
      ]
    }
  }
}
