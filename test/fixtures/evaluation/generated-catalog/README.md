# Generated catalog fixture

Node 20+, no dependencies. data/status.json and scripts/generate.mjs own generated/status.mjs.

```sh
npm run generate
npm test
```

Tests reject stale output and nondeterministic ordering. The baseline contains only ready.
