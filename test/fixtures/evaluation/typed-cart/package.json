{
  "name": "factory-corpus-typed-cart",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build --silent && node check.mjs"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
